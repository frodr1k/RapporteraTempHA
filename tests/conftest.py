"""Test configuration for RapporteraTempHA integration - Bronze minimum."""
import pytest
import sys
from pathlib import Path

# Add the custom_components directory to Python path
sys.path.insert(0, str(Path(__file__).parent.parent))


